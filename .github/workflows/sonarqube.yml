# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow helps you trigger a SonarQube analysis of your code and populates
# GitHub Code Scanning alerts with the vulnerabilities found.
# (this feature is available starting from SonarQube 9.7, Developer Edition and above)

# 1. Make sure you add a valid GitHub configuration to your SonarQube (Administration > DevOps platforms > GitHub)

# 2. Import your project on SonarQube
#     * Add your repository as a new project by clicking "Create project" from your homepage.
#
# 3. Select GitHub Actions as your CI and follow the tutorial
#     * a. Generate a new token and add it to your GitHub repository's secrets using the name SONAR_TOKEN
#          (On SonarQube, click on your avatar on top-right > My account > Security or ask your administrator)
#
#     * b. Copy/paste your SonarQube host URL to your GitHub repository's secrets using the name SONAR_HOST_URL
#
#     * c. Copy/paste the project Key into the args parameter below
#          (You'll find this information in SonarQube by following the tutorial or by clicking on Project Information at the top-right of your project's homepage)

# Feel free to take a look at our documentation (https://docs.sonarqube.org/latest/analysis/github-integration/)
# or reach out to our community forum if you need some help (https://community.sonarsource.com/c/sq/10)

name: SonarQube analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  pull-requests: read # allows SonarQube to decorate PRs with analysis results

jobs:
  Analysis:
    runs-on: ubuntu-latest

    env:
      SONAR_HOST_URL: https://sonarcloud.io
      SONAR_ORGANIZATION: badmade
      SONAR_PROJECT_KEY: badmade_tiingo_data_pull

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install SonarScanner CLI
        run: |
          SONAR_SCANNER_VERSION=7.3.0.5189
          curl -sSLo sonar-scanner.zip "https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-${SONAR_SCANNER_VERSION}.zip"
          unzip -q sonar-scanner.zip
          mv sonar-scanner-* sonar-scanner
          rm sonar-scanner.zip

      - name: Assert Sonar target (safe echo)
        run: |
          echo "SONAR_HOST_URL=${SONAR_HOST_URL:-<unset>}"
          echo "SONAR_ORG=${SONAR_ORGANIZATION:-<unset>}"
          echo "PROJECT_KEY=${SONAR_PROJECT_KEY:-<unset>}"

      - name: Analyze with SonarQube
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}   # Generate a token on SonarQube, add it to the secrets of this repo with the name SONAR_TOKEN (Settings > Secrets > Actions > add new repository secret)
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed for PR decoration
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_BRANCH_NAME: ${{ github.head_ref }}
          PR_BASE_BRANCH: ${{ github.base_ref }}
        run: |
          PR_ARGS=()

          # Add pull request parameters when running on a PR
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            PR_ARGS+=("-Dsonar.pullrequest.key=${PR_NUMBER}")
            PR_ARGS+=("-Dsonar.pullrequest.branch=${PR_BRANCH_NAME}")
            PR_ARGS+=("-Dsonar.pullrequest.base=${PR_BASE_BRANCH}")
          fi

          ./sonar-scanner/bin/sonar-scanner \
            -Dsonar.host.url="${SONAR_HOST_URL}" \
            -Dsonar.organization="${SONAR_ORGANIZATION}" \
            -Dsonar.projectKey="${SONAR_PROJECT_KEY}" \
            -Dsonar.login="${SONAR_TOKEN}" \
            "${PR_ARGS[@]}"
